# Concise wording variants of four authored checkups

Frozen before generation. The user requests stronger or differently framed
versions of all four prior interventions without drastically increasing length.
The root authors one new version of each; the combined version is the exact
concatenation of the new correction and development notes. No examples of
character dialogue, reduced biographies, editor calls or production changes.

Use precisely the prior Breakwater checkpoint: published turn 18 of the continued
Sol checkup-enabled branch, archived at
`78caed4e7fe50523097452dc95a30d6ed7c82d6a`. It is Tuesday lunch after the archive
induction, before Owen's Wednesday interview-reply deadline. All arms inherit
the same 18 passages and three prior checkup exchanges, excluding subsequent
events. Verify all 21 native user messages and final outputs before generation.
The original notes and results remain unchanged at archive commit
`7e324168435acbf852c42f1702ac7ce3d803431b`.

Run five conditions twice, with six identical subsequent player inputs each:
no-note control; firmer prose correction; stronger causal character planning;
their exact combination; and a differently framed ensemble creative brief.
Keep each note at or below the original's word count. This is a comparison of
complete guidance strategies, not an isolated-word or equal-token experiment.

Inject each note once in the same user-role backstage_guidance block preceding
the first action. The control receives only the action/task. Notes remain advice,
not established facts or NPC knowledge. Each subsequent action continues that
native conversation. Use Sol/max, detailed exposed summaries, disabled tools,
full frozen biographies, and at most three concurrent author turns hosted by one
app-server. No further automatic checkups occur in these six turns.

Retain every first completed output and every failed attempt. Only a confirmed
serverOverloaded failure with no reasoning or prose may be retried, after a
verified clean fork through the preceding successful turn. The retained recovery
helper provides that narrow path; unknown outcomes and partial generated content
halt for inspection. Transport restarts must resume existing native histories.

Assign masked condition labels independently within each replication. The root
reads every complete new continuation before unmasking and compares it with the
original trajectories after fixing its judgments. A fresh independent reader
receives all twenty randomly relabelled original and new continuations, full
common canon and published history, but no notes, conditions, batch identities
or prior reviews. Freeze both assessments before revealing the mapping. The root
remembers earlier prose, so its masking is partial even before unmasking.

Judge whole exchanges for enjoyable, particular dialogue; independently motivated
people; development enacted through choices and consequences; room for player
replies; appropriate time skips and quiet endings; and less narration certifying
compliance. Check actual knowledge, chronology, access restrictions and player
ownership. Consistent new history and offstage private decisions are valid. Do
not equate shorter prose, more events, fewer words from a phrase list, or absence
of visible progress with better writing or a logical contradiction.

Compare each variant with its original as well as the new controls, report
replica disagreements and regressions, and distinguish instruction persistence
from first-turn effects. Old originals were generated in the previous batch;
contemporaneous controls help contextualize variation but cannot remove all
batch or stochastic effects. One selected checkpoint, two samples per strategy
and a quiet fixed script do not establish transfer or long-term improvement.

Archive all source hashes, exact requests, public event streams, exposed summaries,
final native histories, assessments and mapping. Do not inspect private reasoning.
Verify live story files are unchanged. Do not promote a production prompt merely
because a selected passage is better.
